# ================================================================
# DEPRECATED / UNUSED CONFIG
# ================================================================
# This config file is NOT currently used by the system.
# The RAG embedding model is configured directly in rag_configs/*.yaml
# via the `emb_model_id` field (currently set to "jina-api").
#
# This file is retained for potential future use if the embedding
# configuration is migrated to the llm_config system.
# ================================================================

name: embedding_model
base_llm_config: base_default_llm
type: llm_config
description_for_human: "[UNUSED] LLM config for text embedding models. See rag_configs/*.yaml for actual embedding configuration."

config:
  model:
    _type: "from_env"
    var: "EMBEDDING_LLM"
    default: "gemini/gemini-embedding-exp-03-07"  # OUTDATED - actual system uses jina-api
    required: false
