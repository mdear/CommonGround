# CommonGround Core - Python Package Configuration
#
# To regenerate requirements.txt from this file:
#   uv export --no-hashes -o requirements.txt
#
# To regenerate with dev dependencies:
#   uv export --no-hashes --extra dev -o requirements-dev.txt
#
# To upgrade all packages and regenerate:
#   uv lock --upgrade && uv export --no-hashes -o requirements.txt

[project]
name = "common-ground-agent-core"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
license = {text = "Apache-2.0"}
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.8.0",
    "anthropic>=0.40.0",
    "beautifulsoup4>=4.9.3",
    "openai>=1.0.0",
    "litellm>=1.30.0",
    "pydantic>=2.10.6",
    "python-dotenv>=1.0.0",
    "python-json-logger>=2.0.7",
    "requests>=2.28.0",
    "markdown>=3.4.0",
    "coolname>=1.1.0",
    "numpy>=2",
    "numba==0.61.2",
    "json-repair==0.40.0",
    "pyyaml>=6.0",
    "fastapi>=0.109.0",
    "uvicorn>=0.27.0",
    "websockets>=12.0",
    "pygls==1.3.1",
    "lsprotocol==2023.0.1",
    "google-genai",
    "lark>=1.2.2",
    "querystring-parser>=1.2.4",
    "aiosqlite>=0.21.0",
    "aiofiles>=24.1.0",
    "mcp>=0.0.1", # Note: version is lower than before, please confirm if it's an internal package
    "duckdb==1.3.1",
    "faiss-cpu>=1.7.0",
    "colorama>=0.4.6",
    "watchdog",
    "cleantext>=1.1.0",
    "langchain-community>=0.0.13",
    "langchain-text-splitters>=0.0.1",
    "langchain-openai>=0.0.2",
    "pymupdf>=1.23.0",
    "tavily-python>=0.3.0",
    "yt-dlp>=2023.12.30",
    "markitdown[all]==0.1.2",
    "pocketflow>=0.0.1",
    "fastmcp>=0.0.1",
    "light-embed @ git+https://github.com/chux0519/light-embed@b98fc0f1262e0fa4a140f9b2a437b83f79d4158e",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.1.0",
    "pytest-asyncio>=0.23.0",
    "pip-tools>=7.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
asyncio_mode = "auto"
addopts = "-v --tb=short"

[tool.coverage.run]
source = ["agent_core"]
omit = ["*/tests/*", "*/__pycache__/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
    "raise NotImplementedError",
]
